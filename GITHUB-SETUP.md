# GitHub setup

```powershell
git init
git add .
git commit -m "feat: ReviveX agentic revenue recovery platform"
git branch -M main
git remote add origin YOUR_PUBLIC_REPO_URL
git push -u origin main
```

Do not commit secrets. The demo uses synthetic data and no API credentials.
