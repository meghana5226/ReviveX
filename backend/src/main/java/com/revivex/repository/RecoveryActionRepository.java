package com.revivex.repository; import com.revivex.domain.*; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface RecoveryActionRepository extends JpaRepository<RecoveryAction,Long>{Optional<RecoveryAction> findByIdempotencyKey(String key); List<RecoveryAction> findByStatus(String status); List<RecoveryAction> findByRecoveryCaseId(Long id); }
